# Indian-Kabaadiya
An online waste pickup project

Set-up on local:
1.Clone the repository
2.Run npm install
3.Rename config.example.js to config.js and add credentials for mongoDB
4.Run node server.js

Browse localhost:3000
